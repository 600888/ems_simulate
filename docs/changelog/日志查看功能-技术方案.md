# 系统日志查看功能 - 技术方案

## 1. 概述

在 EMS Simulate 系统右上角添加日志按钮，点击后以弹框形式展示系统日志，支持按模块、按设备筛选，日志仅显示日期和内容。

## 2. 功能需求

- **入口**：右上角导航栏新增日志图标按钮
- **展示形式**：Element Plus Dialog 弹框，覆盖在主界面之上
- **日志筛选**：
  - 按模块筛选：系统、Web服务、IEC61850、IEC104、模拟器
  - 按设备筛选：选择"系统"模块时，可进一步筛选具体设备
- **日志显示**：仅显示日期时间 + 日志内容，按时间倒序排列
- **自动刷新**：支持开关自动刷新（默认关闭），轮询间隔 5 秒
- **日志来源**：读取后端日志文件

## 3. 后端方案

### 3.1 新增 API 端点

在 FastAPI 中新增 `/api/logs` 路由：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/logs/modules` | 获取可用日志模块列表 |
| GET | `/api/logs/devices` | 获取有独立日志的设备列表 |
| GET | `/api/logs/query` | 查询日志内容（支持分页、筛选） |

### 3.2 日志文件映射

| 模块名称 | 日志文件路径 | 说明 |
|----------|-------------|------|
| `system` | `{LOG_DIR}/ems_simulate.log` | 系统主日志 |
| `web` | `{LOG_DIR}/web.log` | Web 服务日志 |
| `iec61850` | `{LOG_DIR}/iec61850.log` | IEC 61850 协议日志 |
| `iec104` | `{LOG_DIR}/iec104.log` | IEC 104 协议日志 |
| `simulator` | `{LOG_DIR}/simulate.log` | 模拟器日志 |
| `device` | `{LOG_DIR}/{device_name}/{device_name}.log` | 各设备独立日志 |

### 3.3 日志读取逻辑

- 使用 `loguru` 的日志格式为：`[时间] [模块:函数:行号] [级别] 消息`
- 后端读取日志文件，按行解析，提取时间戳和消息内容
- 支持 `offset`（偏移行数）和 `limit`（返回条数）实现分页
- 支持 `module` 和 `device` 筛选参数
- 支持 `keyword` 关键词搜索
- 按时间倒序返回（最新的在前）

### 3.4 API 响应格式

```json
// GET /api/logs/modules
{
  "modules": ["system", "web", "iec61850", "iec104", "simulator", "device"]
}

// GET /api/logs/devices
{
  "devices": ["RTU-01", "RTU-02", "IED-01"]
}

// GET /api/logs/query?module=system&device=&offset=0&limit=100&keyword=
{
  "total": 1024,
  "offset": 0,
  "limit": 100,
  "logs": [
    { "time": "2026-07-06 14:32:15", "content": "设备 [RTU-01] 连接成功" },
    { "time": "2026-07-06 14:32:14", "content": "IEC 61850 服务器启动完成" }
  ]
}
```

## 4. 前端方案

### 4.1 新增文件

| 文件路径 | 说明 |
|---------|------|
| `front/src/api/logApi.ts` | 日志 API 请求模块 |
| `front/src/components/logs/LogViewerDialog.vue` | 日志查看器弹框组件 |
| `front/src/composables/useLogViewer.ts` | 日志查看逻辑 composable |

### 4.2 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `front/src/components/header/AppHeader.vue` | 在 `link-container` 中添加日志按钮 |
| `front/src/i18n/locales/zh-CN.ts` | 添加日志相关翻译 |
| `front/src/i18n/locales/en-US.ts` | 添加日志相关翻译 |
| `src/web/app.py` | 注册日志路由 |
| `src/web/routers/` | 新增 `log_router.py` |

### 4.3 组件设计：LogViewerDialog.vue

```
┌──────────────────────────────────────────────────┐
│  📋 系统日志                              [关闭] │
├──────────────────────────────────────────────────┤
│  [模块: ▼] [设备: ▼] [🔍 搜索...] [🔄] [⏱ 自动刷新] │
├──────────────────────────────────────────────────┤
│  2026-07-06 14:32:15  设备 [RTU-01] 连接成功      │
│  2026-07-06 14:32:14  IEC 61850 服务器启动完成     │
│  2026-07-06 14:32:10  Web 服务已启动，端口: 8991   │
│  2026-07-06 14:31:55  系统初始化完成                │
│  ...                                               │
├──────────────────────────────────────────────────┤
│  共 1,024 条日志                         [上一页] [下一页] │
└──────────────────────────────────────────────────┘
```

### 4.4 交互流程

1. 用户点击右上角日志图标按钮
2. 弹框打开，自动加载最新日志（默认选择"全部模块"）
3. 用户选择模块筛选器 → 触发重新查询
4. 若选择"系统"模块 → 启用设备筛选器
5. 用户开启自动刷新 → 每 5 秒轮询一次最新日志
6. 点击关闭按钮或按 ESC 关闭弹框

### 4.5 国际化新增翻译

**zh-CN:**
```
log.viewer: "系统日志"
log.module.all: "全部模块"
log.module.system: "系统"
log.module.web: "Web服务"
log.module.iec61850: "IEC61850"
log.module.iec104: "IEC104"
log.module.simulator: "模拟器"
log.device.all: "全部设备"
log.total: "共 {count} 条日志"
log.autoRefresh: "自动刷新"
log.search: "搜索日志"
```

## 5. 数据结构

```typescript
// LogEntry
interface LogEntry {
  time: string    // "2026-07-06 14:32:15"
  content: string // "设备 [RTU-01] 连接成功"
}

// LogQueryParams
interface LogQueryParams {
  module?: string
  device?: string
  offset: number
  limit: number
  keyword?: string
}

// LogQueryResponse
interface LogQueryResponse {
  total: number
  offset: number
  limit: number
  logs: LogEntry[]
}
```

## 6. 后端实现细节

### 日志路由文件：`src/web/routers/log_router.py`

```python
import os
from fastapi import APIRouter, Query
from src.config.global_config import LOG_DIR
from src.config.log.device_logger import DeviceLoggerManager

router = APIRouter(prefix="/api/logs", tags=["logs"])

# 模块 -> 日志文件映射
MODULE_LOG_FILES = {
    "system": "ems_simulate.log",
    "web": "web.log",
    "iec61850": "iec61850.log",
    "iec104": "iec104.log",
    "simulator": "simulate.log",
}
```

### 日志文件读取

- 使用 `open()` 读取日志文件
- 使用 `re` 正则解析 loguru 格式的时间戳
- 支持大文件优化：从文件末尾向前读取
- 按文件修改时间排序

## 7. 实施计划

| 步骤 | 内容 | 工作量 |
|------|------|--------|
| 1 | 后端：新增 log_router.py，实现日志读取 API | 小 |
| 2 | 后端：在 app.py 中注册日志路由 | 极小 |
| 3 | 前端：创建 logApi.ts | 极小 |
| 4 | 前端：创建 LogViewerDialog.vue 组件 | 中 |
| 5 | 前端：修改 AppHeader.vue 添加日志按钮 | 小 |
| 6 | 前端：添加国际化翻译 | 小 |
| 7 | 联调测试 | 小 |
